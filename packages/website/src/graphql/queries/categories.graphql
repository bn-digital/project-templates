query categories($sort: [String] = ["id:DESC"]) {
  categories(sort: $sort) {
    data {
      ...Category
      attributes {
        posts {
          data {
            ...Post
          }
        }
      }
    }
  }
}
