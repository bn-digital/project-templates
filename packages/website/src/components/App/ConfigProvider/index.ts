import {
  ConfigProvider,
  primaryLightColor,
  primaryDarkColor,
  accentColor,
  ConfigProviderProps,
  ThemeConfig,
  AppThemeToken,
} from './ConfigProvider';

export type {
  ConfigProviderProps,
  ThemeConfig,
  AppThemeToken,
};

export {
  primaryDarkColor,
  primaryLightColor,
  accentColor,
};

export default ConfigProvider;
