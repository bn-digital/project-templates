{
  "kind": "collectionType",
  "collectionName": "emails",
  "info": {
    "singularName": "email",
    "pluralName": "emails",
    "displayName": "Email",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "email": {
      "type": "email"
    },
    "template": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "plugin::email-designer.email-template"
    },
    "payload": {
      "type": "json",
      "required": false
    },
    "status": {
      "type": "enumeration",
      "enum": ["new", "queued", "sent", "failed"],
      "default": "new",
      "required": true,
      "enumName": "EmailStatus"
    }
  }
}
