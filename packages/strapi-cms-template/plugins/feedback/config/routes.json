{
  "routes": [
    {
      "method": "POST",
      "path": "/contact",
      "handler": "feedback.contact.create",
      "config": {
        "policies": [
          "plugins::users-permissions.ratelimit"
        ]
      }
    }
  ]
}
